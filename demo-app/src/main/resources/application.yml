server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/op_demo?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: never
      encoding: UTF-8
logging:
  level:
    root: info
    com.example: info
    businessLog: info
  file:
    name: logs/demo.log
